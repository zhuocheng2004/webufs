
import {
    FileSystemType, 
    VFS, defaultVFS, 
    Context, createDefaultContext,
    InMemoryFSType, InMemoryFS 
} from "@webufs/webufs"

const webufs = {
    FileSystemType,
    VFS, defaultVFS,
    Context, createDefaultContext,
    InMemoryFSType, InMemoryFS
}

export default webufs
