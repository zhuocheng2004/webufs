
import {
    FileSystemType, SeekType,
    VFS, defaultVFS, 
    Context, createDefaultContext,
    InMemoryFSType, InMemoryFS 
} from "@webufs/webufs"

const webufs = {
    FileSystemType, SeekType,
    VFS, defaultVFS,
    Context, createDefaultContext,
    InMemoryFSType, InMemoryFS
}

export default webufs
