
import {
    FileSystemType, 
    VFS, defaultVFS, 
    Context, defaultContext,
    InMemoryFSType, InMemoryFS 
} from "@webufs/webufs"

const webufs = {
    FileSystemType,
    VFS, defaultVFS,
    Context, defaultContext,
    InMemoryFSType, InMemoryFS
}

export default webufs
